# 构建和工具脚本

本目录包含用于构建、版本管理和发布的自动化脚本。

## 脚本列表

### build.sh - macOS/Linux 构建脚本

**用途**：在 macOS 或 Linux 上构建 qsl-cardhub 应用。

**使用方法**：

```bash
./scripts/build.sh
```

**功能**：
- 自动检查依赖（Node.js、npm、Rust、cargo）
- 验证版本号一致性（Cargo.toml vs tauri.conf.json）
- 构建前端（npm run build）
- 打包 Tauri 应用（cargo tauri build）
- 复制产物到 dist/ 目录
- 显示构建摘要和文件大小

**输出**：
- macOS: `dist/qsl-cardhub-v{version}-macos-universal.dmg`

**前置要求**：
- Node.js 18+
- npm
- Rust 1.70+
- cargo
- macOS: Xcode Command Line Tools

**示例输出**：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  qsl-cardhub 构建脚本
  版本: 0.1.0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

检查依赖...
✓ Node.js: v20.10.0
✓ npm: 10.2.3
✓ Rust: 1.75.0
✓ cargo: 1.75.0
✓ 所有依赖已满足

构建前端...
✓ 前端构建成功

打包 Tauri 应用...
✓ Tauri 应用打包成功

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  构建完成！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📦 产物:
  dist/qsl-cardhub-v0.1.0-macos-universal.dmg (15.2 MB)
```

---

### build.ps1 - Windows 构建脚本

**用途**：在 Windows 上构建 qsl-cardhub 应用。

**使用方法**：

```powershell
.\scripts\build.ps1
```

或使用 CMD 入口：

```cmd
.\scripts\build.bat
```

**功能**：
- 自动检查依赖（Node.js、npm、Rust、cargo）
- 自动检测系统架构（x64 或 ARM64）
- 验证版本号一致性
- 构建前端
- 打包 Tauri 应用
- 复制产物到 dist\ 目录
- 显示构建摘要和文件大小

**输出**：
- Windows x64: `dist\qsl-cardhub-v{version}-windows-x64.msi`
- Windows ARM64: `dist\qsl-cardhub-v{version}-windows-arm64.msi`

**前置要求**：
- Node.js 18+
- npm
- Rust 1.70+
- cargo
- PowerShell 5.0+

**示例输出**：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  qsl-cardhub 构建脚本
  版本: 0.1.0
  架构: x64
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[√] 检查依赖...
[√] Node.js: v20.10.0
[√] npm: 10.2.3
[√] Rust: 1.75.0
[√] cargo: 1.75.0
[√] 所有依赖已满足

[√] 构建前端...
[√] 前端构建成功

[√] 打包 Tauri 应用...
[√] Tauri 应用打包成功

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  构建完成！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

产物:
  dist\qsl-cardhub-v0.1.0-windows-x64.msi (12.8 MB)
```

---

### build.bat - Windows CMD 入口

**用途**：提供 CMD 环境下的入口，调用 PowerShell 脚本。

**使用方法**：

```cmd
.\scripts\build.bat
```

**说明**：
- 自动检查 PowerShell 是否可用
- 调用 build.ps1 执行实际构建
- 传递退出码

---

### sync-version.sh - 版本同步脚本

**用途**：将版本号从 Cargo.toml 同步到 tauri.conf.json。

**使用方法**：

```bash
./scripts/sync-version.sh
```

**功能**：
- 从 Cargo.toml 读取版本号
- 验证版本号格式（semver: X.Y.Z）
- 更新 tauri.conf.json 中的版本号
- 显示同步前后的版本对比

**前置要求**：
- bash
- sed (macOS/Linux)

**使用场景**：
1. 修改 Cargo.toml 中的版本号后
2. 创建新版本发布前
3. 验证版本一致性时

**示例输出**：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  qsl-cardhub 版本同步工具
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ℹ 从 Cargo.toml 读取版本号: 0.2.0
ℹ tauri.conf.json 当前版本: 0.1.0

ℹ 正在更新 tauri.conf.json...
✓ 版本号已同步: 0.1.0 → 0.2.0

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ 同步完成！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### version.sh - 版本管理脚本

**用途**：一键升级版本号并同步到所有配置文件。

**使用方法**：

```bash
./scripts/version.sh [命令]
```

**命令**：
- `(无参数)` - 显示当前版本号
- `major` - 升级主版本号 (1.0.0 → 2.0.0)
- `minor` - 升级次版本号 (1.0.0 → 1.1.0)
- `patch` - 升级补丁版本号 (1.0.0 → 1.0.1)
- `x.y.z` - 设置自定义版本号
- `check` - 检查所有文件的版本一致性
- `sync` - 从 Cargo.toml 同步版本到其他文件

**功能**：
- 从 Cargo.toml 读取当前版本
- 自动计算新版本号（major/minor/patch）
- 同步更新 Cargo.toml、tauri.conf.json、web/package.json
- 验证 semver 格式

**前置要求**：
- bash
- sed (macOS/Linux)

**示例**：

```bash
# 显示当前版本
./scripts/version.sh

# 升级补丁版本 (0.3.0 → 0.3.1)
./scripts/version.sh patch

# 升级次版本 (0.3.0 → 0.4.0)
./scripts/version.sh minor

# 设置自定义版本
./scripts/version.sh 1.0.0

# 检查版本一致性
./scripts/version.sh check
```

**示例输出**：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  qsl-cardhub 版本升级
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ℹ 当前版本: 0.3.0
ℹ 新版本:   0.3.1

✓ 已更新 Cargo.toml
✓ 已更新 tauri.conf.json
✓ 已更新 web/package.json

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ 版本升级完成: 0.3.0 → 0.3.1
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### release.sh - 发布脚本

**用途**：一键发布新版本，整合版本升级、构建验证和 Git 操作。

**使用方法**：

```bash
./scripts/release.sh [版本类型]
```

**参数**：
- `major` - 主版本升级 (1.0.0 → 2.0.0)
- `minor` - 次版本升级 (1.0.0 → 1.1.0)
- `patch` - 补丁版本升级 (1.0.0 → 1.0.1)
- `x.y.z` - 自定义版本号

**完整流程**：
1. 检查 Git 仓库状态
2. 检查当前分支（非主分支会提示确认）
3. 检查工作区是否干净
4. 拉取最新代码
5. 调用 version.sh 升级版本号
6. 运行 `cargo check` 验证 Rust 代码
7. 创建版本提交
8. 创建 Git 标签
9. 推送到远程（可选）

**前置要求**：
- bash
- git
- cargo (Rust)

**示例**：

```bash
# 发布补丁版本
./scripts/release.sh patch

# 发布次版本
./scripts/release.sh minor

# 发布自定义版本
./scripts/release.sh 1.0.0
```

**示例输出**：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  qsl-cardhub 发布工具
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ Git 仓库检查通过
✓ 当前分支: master
✓ 工作区干净
✓ 已拉取最新代码

ℹ 升级版本号...
  [version.sh 输出]

ℹ 运行构建验证...
✓ Rust 代码检查通过

ℹ 创建版本提交...
✓ 已创建提交: chore: bump version to 0.4.0

ℹ 创建 Git 标签: v0.4.0
✓ 已创建标签: v0.4.0

是否推送到远程仓库? [Y/n] y
ℹ 推送到远程...
✓ 已推送到远程

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ 发布准备完成: v0.4.0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 工作流程

### 本地开发构建

1. **开发模式**（热重载）：
   ```bash
   cargo tauri dev
   ```

2. **生产构建**（完整打包）：
   ```bash
   # macOS/Linux
   ./scripts/build.sh

   # Windows
   .\scripts\build.ps1
   ```

### 版本发布流程

#### 方式一：使用一键发布脚本（推荐）

```bash
# 发布补丁版本 (0.3.0 → 0.3.1)
./scripts/release.sh patch

# 发布次版本 (0.3.0 → 0.4.0)
./scripts/release.sh minor

# 发布主版本 (0.3.0 → 1.0.0)
./scripts/release.sh major

# 发布自定义版本
./scripts/release.sh 1.0.0
```

脚本会自动完成：版本升级 → 构建验证 → 提交 → 打标签 → 推送

#### 方式二：手动发布

1. **更新版本号**：
   ```bash
   ./scripts/version.sh patch  # 或 minor/major/x.y.z
   ```

2. **提交更改**：
   ```bash
   git add Cargo.toml tauri.conf.json web/package.json
   git commit -m "chore: bump version to 0.4.0"
   git push origin master
   ```

3. **创建发布标签**：
   ```bash
   git tag v0.4.0
   git push origin v0.4.0
   ```

4. **自动构建和发布**：
   - GitHub Actions 会自动触发
   - 在 macOS、Windows x64 和 Windows ARM64 上并行构建
   - 创建 GitHub Release
   - 上传构建产物

### PR 构建验证

创建 Pull Request 到 master 分支时，会自动触发构建验证：

```bash
git checkout -b feature/my-feature
git add .
git commit -m "feat: add new feature"
git push origin feature/my-feature
# 在 GitHub 上创建 PR，构建将自动开始
```

---

## 故障排查

### 依赖缺失

**症状**：脚本报告缺少依赖

**解决**：
```bash
# 安装 Node.js (使用 nvm)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 20

# 安装 Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

### 版本号不一致

**症状**：脚本警告版本号不匹配

**解决**：
```bash
./scripts/sync-version.sh
```

### 构建失败

**症状**：前端或 Tauri 构建失败

**调试步骤**：
1. 检查错误日志
2. 验证依赖版本：
   ```bash
   node --version  # 应为 18+
   npm --version
   rustc --version  # 应为 1.70+
   ```
3. 清理缓存并重试：
   ```bash
   # 清理前端缓存
   cd web
   rm -rf node_modules dist
   npm install

   # 清理 Rust 缓存
   cd ..
   cargo clean

   # 重新构建
   ./scripts/build.sh
   ```

### Windows 执行策略限制

**症状**：PowerShell 脚本无法执行

**错误信息**：
```
.\build.ps1 : 无法加载文件，因为在此系统上禁止运行脚本
```

**解决**：
```powershell
# 临时允许（当前会话）
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process

# 或使用 CMD 入口（推荐）
.\scripts\build.bat
```

### macOS sed 兼容性问题

**症状**：sync-version.sh 在 Linux 上失败

**原因**：macOS 和 Linux 的 sed 语法略有不同

**解决**：脚本已处理此问题，使用 `$OSTYPE` 检测平台

---

## 脚本开发

### 修改脚本

修改脚本后，确保：

1. **权限正确**：
   ```bash
   chmod +x scripts/*.sh
   ```

2. **格式一致**：
   - bash 脚本使用 LF 换行符
   - PowerShell 脚本使用 CRLF 换行符

3. **测试所有平台**：
   - macOS: 本地测试
   - Windows: 本地测试或 GitHub Actions
   - Linux: GitHub Actions（如需要）

### 添加新脚本

创建新脚本时：

1. 添加到 `scripts/` 目录
2. 使用一致的输出格式（颜色、符号）
3. 包含错误处理（`set -e` 或 `$ErrorActionPreference = "Stop"`）
4. 更新本 README 文档
5. 在 .gitignore 中排除临时文件

---

## 环境变量

脚本使用的环境变量：

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `RUST_BACKTRACE` | Rust 错误回溯 | 脚本中未设置 |
| `CARGO_TERM_COLOR` | Cargo 输出颜色 | 脚本中未设置 |

GitHub Actions 工作流会设置这些变量，本地脚本依赖默认值。

---

## 相关文档

- [GitHub Actions 工作流文档](../.github/workflows/README.md)
- [项目 README](../README.md)
- [Tauri 官方文档](https://tauri.app/)
- [OpenSpec 构建规范](../openspec/changes/build-and-packaging/)

---

## 支持

如有问题或建议，请：

1. 查看本文档的故障排查部分
2. 查看 GitHub Actions 日志
3. 提交 Issue 到项目仓库
