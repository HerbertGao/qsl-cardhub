## 1. 导出缓存列实现

- [x] 1.1 在 `src/commands/export.rs` 中新增三列表头（QRZ.cn(中文)、QRZ.cn(English)、QRZ.com）并更新列顺序。
- [x] 1.2 在数据行写入逻辑中读取 `card.metadata.address_cache`，按来源映射写入三列。
- [x] 1.3 对缺失缓存或字段缺失场景写入空字符串，不进行 URL 拼接。
- [x] 1.4 调整新增三列列宽，保证导出可读性。

## 2. 测试与回归

- [x] 2.1 新增/更新单元测试，覆盖缓存命中、缺失缓存、部分缓存三种场景。
- [x] 2.2 验证原有导出行为（数量模式、状态映射、文件名规则）无回归。

## 3. 一致性检查

- [x] 3.1 核对实现与变更规范一致：仅使用缓存填充，不触发在线查询。
- [x] 3.2 记录导出对缓存时效的依赖：地址缓存有效性规则仍由现有缓存模块维护。
