# 任务列表：过滤隐藏配置文件

## 任务状态说明
- `[ ]` 待开始
- `[→]` 进行中
- `[x]` 已完成
- `[!]` 已阻塞

---

## 阶段 1: 代码实现 (预计: 15分钟)

### 1.1 修改 ProfileManager::get_all() 方法
- [x] 在文件名检查逻辑中添加过滤条件
- [x] 跳过以 `.` 开头的文件名
- [x] 确保逻辑简洁且易于理解

**依赖**: 无
**验证**: 手动测试 + 单元测试

### 1.2 重命名示例配置文件
- [x] 使用 `git mv` 将 `config/profiles/example.toml` 重命名为 `config/profiles/.example.toml`
- [x] 验证文件内容未被修改

**依赖**: 无
**验证**: 文件系统检查

---

## 阶段 2: 测试 (预计: 10分钟)

### 2.1 添加单元测试
- [x] 添加测试：验证隐藏文件不被 `get_all()` 返回
- [x] 添加测试：验证正常文件仍被 `get_all()` 返回
- [x] 添加测试：验证空目录情况

**依赖**: 1.1
**验证**: `cargo test`

### 2.2 运行现有测试
- [x] 运行 `cargo test` 确保所有现有测试通过
- [x] 检查是否有测试依赖 `example.toml` 的存在

**依赖**: 1.2
**验证**: 测试输出

---

## 阶段 3: 集成验证 (预计: 5分钟)

### 3.1 手动功能测试
- [x] 启动应用，检查配置列表不包含 `.example.toml`
- [x] 创建新的配置，验证可以正常显示
- [x] 删除配置，验证列表更新正确

**依赖**: 2.2
**验证**: 手动测试

### 3.2 边界情况测试
- [x] 测试 profiles 目录为空的情况
- [x] 测试目录中只有隐藏文件的情况
- [x] 测试目录中混合隐藏和正常文件的情况

**依赖**: 3.1
**验证**: 手动测试 + 日志检查

---

## 阶段 4: 文档更新 (预计: 5分钟)

### 4.1 更新代码注释
- [x] 在 `get_all()` 方法中添加注释说明隐藏文件过滤逻辑

**依赖**: 1.1
**验证**: 代码审查

---

## 总计

- **总任务数**: 11
- **预计总时间**: 35分钟
- **并行可能**: 阶段 1 的两个任务可并行执行

---

## 验收检查清单

在合并前确认：
- [x] 所有任务标记为 `[x]`
- [x] `cargo test` 通过（100%）
- [x] `cargo clippy` 无警告
- [x] `git status` 显示 `example.toml` 已删除，`.example.toml` 已添加
- [x] 手动测试确认配置列表不显示隐藏文件
